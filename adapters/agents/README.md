# `.agents/skills` adapter

Grok (and other Agent Skills clients) scan `.agents/skills/` at the repo root. Install copies the three skill packages here. This directory in the method pack is documentation only until install runs (or you copy by hand).
