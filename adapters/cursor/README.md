# Cursor adapter

Copy `cranky.mdc` to the consumer's `.cursor/rules/cranky.mdc` if you want a Cursor rule in addition to Agent Skills. Install script copies skills to `.agents/skills/`; this `.mdc` is optional extra.

Keep this file a pointer. Do not duplicate FORMAT.md here.
