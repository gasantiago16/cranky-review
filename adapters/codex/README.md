# Codex adapter

Codex loads `AGENTS.md`. After install, skills also sit in `.agents/skills/`.

If Codex will not spawn a child: open a **new** thread, paste `skills/cranky/persona.md`, point it at the diff. Same-thread review of your own patch is not cranky.

No second copy of the taxonomy here.
