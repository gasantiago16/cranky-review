# Environment notes

- GitHub repo `gasantiago16/cranky-review` is **private**. MCP `create_repository` returned 403; `gh repo create` worked with the user token (`repo` scope).
- Workspace clone: local `Projects/cranky-review`.
- Install on Windows: `.\scripts\install.ps1 -Target .`
