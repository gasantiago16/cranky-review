# Claude Code adapter

Do not fork the method into Claude-only prose.

Install copies `skills/*` to `.claude/skills/`. Optional: copy `commands/*.md` so `/cranky` is a slash command.

`cranky-reviewer.md` is a named-agent stub. It only says: read `persona.md` from the skill package. Keep it that thin so FORMAT.md stays the schema home.

This pack's root `CLAUDE.md` is a pointer to `AGENTS.md`.
