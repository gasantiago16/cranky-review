# Overlays (consumer)

Drop `overlay-<product>.md` files here. Cranky loads every `*.md` in this directory except this README.

See the method pack's `docs/OVERLAYS.md` and `overlay.template.md` in this folder.
